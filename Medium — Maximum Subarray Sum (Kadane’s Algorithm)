def maxSubArray(nums):
    best = curr = nums[0]
    for n in nums[1:]:
        curr = max(n, curr + n)
        best = max(best, curr)
    return best

# Example
print(maxSubArray([-2,1,-3,4,-1,2,1,-5,4]))  # 6 ([4,-1,2,1])
